{
  "name": "zeth-truffle",
  "version": "1.0.0",
  "description": "zerocash on Ethereum",
  "main": "truffle.js",
  "scripts": {
    "testrpc": "ganache-cli --port 8545 --gasLimit 0xFFFFFFFFFFF --gasPrice 1 --defaultBalanceEther 99999999999 --networkId 1234",
    "compile": "truffle compile",
    "deploy": "truffle deploy",
    "test": "truffle test",
    "debug": "truffle debug",
    "coverage": "solidity-coverage",
    "genbin": "solc --overwrite --bin ./contracts/*.sol -o abi",
    "genabi": "solc --overwrite --abi ./contracts/*.sol -o abi"
  },
  "author": "Clearmatics Technologies LTD: Cryptography R&D Department",
  "license": "LGPL-3.0+",
  "dependencies": {
    "add": "^2.0.6",
    "web3": "^1.0.0-beta.37"
  }
}
